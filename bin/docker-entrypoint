#!/bin/bash

# Replace the environment variables with the proper values
envsubst < /usr/share/logstash/logstash.tpl.conf > /usr/share/logstash/pipeline/logstash.conf

if [[ -z $1 ]] || [[ ${1:0:1} == '-' ]] ; then
  exec logstash "$@"
else
  exec "$@"
fi